version: "3.9"

services:
  chrsolr_web:
    image: node:18.18.2
    container_name: chrsolr-web
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 3000:3000
    volumes:
      - ./src:/app/src
    command: npm start
